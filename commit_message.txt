fix: Resolve webhook and server configuration issues

- Fix API key validation in webhook controller
- Fix MongoDB connection in Docker environment
- Fix webhook request handling and logging
- Fix raw body parsing for webhooks
- Fix JSON parsing for webhook requests
- Fix database connection error handling
- Fix webhook response handling
- Fix environment variable configuration
- Fix Docker networking configuration
- Fix port mappings and service dependencies

This change resolves several issues with webhook handling, database connections, and Docker configuration, ensuring proper functionality in production. The system now correctly:
1. Validates API keys from headers
2. Connects to MongoDB in Docker network
3. Handles webhook requests properly
4. Processes messages correctly
5. Stores data in MongoDB
6. Responds to Evolution API
